# Host inventory for ansible-managed hosts.
# Make sure the ansible_user and ssh key exists on each host.
#
# For more context, see: 
# https://docs.ansible.com/ansible/latest/user_guide/intro_inventory.html
#
all:
  hosts:

    proxmox:
      ansible_host: 10.9.1.10

    servo:
      ansible_host: 10.9.1.200

    homebot:
      ansible_host: 10.9.1.201

    sandbox:
      ansible_host: 10.9.1.223

    tailscale:
      ansible_host: 10.9.1.222
