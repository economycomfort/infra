---
## Random plays for testing.

  - hosts: chunky
    vars_files:
      - 'vars/vault.yaml'
    
    pre_tasks:
      - name: DEBUG show samba_users
        debug:
          msg: "User is {{ item.name }}"
        loop: "{{ samba_users }}"

    roles:
      #- bertvv.samba
      - vladgh.samba.server