---
## Random plays for testing.

  - hosts: pve
    #vars_files:
    #  - 'vars/vault.yaml'
    become: yes

    #tasks:
    #- name: Show info
    #  debug:
    #    msg: "{{ dotfiles_dest }}"

    roles:
      - geerlingguy.ntp
