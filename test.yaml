---
## Random plays for testing.

  - hosts: homebot
    #vars_files:
    #  - 'vars/vault.yaml'
    become: yes

    #tasks:
    #- name: Show info
    #  debug:
    #    msg: "{{ dotfiles_dest }}"

    roles:
      - grog.package
      - { role: dab-dotfiles, become: no }
      #- { role: geerlingguy.dotfiles, become: no }
      #- role: vladgh.samba.server